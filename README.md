# BuildingGun

```mermaid

graph LR;
  ar([AR]);
  up([Upper Receiver]);
  lo([下机匣
  Lower Receiver]);
  barr([Barrel]);
  muzz([Muzzle Device]);
  gas([Gas System]);
  bolt([Bolt Carrier Group]);
  ar --- lo;
  ar --- up;
  lo --- barr;
  lo --- muzz;
  lo --- gas;
  lo --- bolt;
```
